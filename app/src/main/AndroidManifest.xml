<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.software.achilles.tasked" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" >   <!-- NO SOLUCION, solo un apaño -->
            <!--android:windowSoftInputMode="adjustResize" >-->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <activity
            android:name=".view.Preferences"
            android:label="@string/settings"
            android:parentActivityName=".view.MainActivity"
            android:theme="@style/SettingsList">
            <!--Meta-data is used to handle the back button, which right now is Dashboard, but it-->
            <!--won't be forever-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".view.MainActivity" />
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAH_vw0fozTGHKHgnYmsf2whRzxICWoNiM"/>

        <provider
            android:name=".TasksSuggestionProvider"
            android:authorities="com.software.achilles.tasked.TasksSuggestionProvider"
            android:enabled="true"
            android:exported="false"/>
    </application>

</manifest>
